<x-app-layout>
    <x-slot name="header"><div><h1 class="text-2xl font-semibold tracking-tight text-slate-900">System Website Management</h1><p class="mt-1 text-sm text-slate-500">Suspend, activate, and delete websites across all tenants.</p></div></x-slot>
    <div class="min-h-screen bg-slate-50 py-8"><div class="mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8">
        @if(session('success'))<div class="mb-6 rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
        <section class="mb-6 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:p-5"><form method="GET" action="{{ route('admin.system.websites.index') }}" class="grid grid-cols-1 gap-3 lg:grid-cols-[minmax(0,1fr)_220px_auto_auto]"><input type="search" name="search" value="{{ request('search') }}" placeholder="Search website or domain" class="h-11 rounded-xl border-slate-300 text-sm"><select name="status" class="h-11 rounded-xl border-slate-300 text-sm"><option value="">All statuses</option><option value="active" @selected(request('status') === 'active')>Active</option><option value="suspended" @selected(request('status') === 'suspended')>Suspended</option></select><button class="h-11 rounded-xl bg-slate-900 px-5 text-sm font-semibold text-white">Apply</button><a href="{{ route('admin.system.websites.index') }}" class="inline-flex h-11 items-center justify-center rounded-xl border border-slate-200 bg-white px-5 text-sm font-semibold text-slate-600">Clear</a></form></section>
        <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full table-auto border-collapse"><thead class="bg-slate-50"><tr class="border-b border-slate-200"><th class="px-5 py-3.5 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">Website</th><th class="px-5 py-3.5 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">Tenant</th><th class="px-5 py-3.5 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">Knowledge</th><th class="px-5 py-3.5 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">Activity</th><th class="px-5 py-3.5 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">Status</th><th class="px-5 py-3.5 text-right text-xs font-semibold uppercase tracking-wide text-slate-500">Actions</th></tr></thead><tbody class="divide-y divide-slate-100 bg-white">@forelse($websites as $website)<tr class="transition hover:bg-slate-50"><td class="px-5 py-4"><p class="text-sm font-semibold text-slate-900">{{ $website->name }}</p><p class="mt-1 text-xs text-slate-500">{{ $website->domain }}</p></td><td class="px-5 py-4 text-sm text-slate-600">{{ $website->tenant->name ?? '—' }}</td><td class="px-5 py-4 text-sm text-slate-600">{{ number_format($website->knowledge_pages_count) }} pages / {{ number_format($website->knowledge_chunks_count) }} chunks</td><td class="px-5 py-4 text-sm text-slate-600">{{ number_format($website->leads_count) }} leads / {{ number_format($website->conversations_count) }} chats</td><td class="px-5 py-4">@if($website->is_active)<span class="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700 ring-1 ring-emerald-200">Active</span>@else<span class="rounded-full bg-red-50 px-2.5 py-1 text-xs font-semibold text-red-700 ring-1 ring-red-200">Suspended</span>@endif</td><td class="px-5 py-4 text-right"><div class="flex justify-end gap-2"><a href="{{ route('admin.websites.show', $website) }}" class="inline-flex h-9 items-center rounded-lg border border-slate-200 px-3 text-sm font-semibold text-slate-700">View</a>@if($website->is_active)<form method="POST" action="{{ route('admin.system.websites.suspend', $website) }}" onsubmit="return confirm('Suspend this website? Widget access will stop immediately.')">@csrf<button class="inline-flex h-9 items-center rounded-lg border border-amber-200 px-3 text-sm font-semibold text-amber-700">Suspend</button></form>@else<form method="POST" action="{{ route('admin.system.websites.activate', $website) }}">@csrf<button class="inline-flex h-9 items-center rounded-lg border border-emerald-200 px-3 text-sm font-semibold text-emerald-700">Activate</button></form>@endif<form method="POST" action="{{ route('admin.system.websites.destroy', $website) }}" onsubmit="return confirm('Delete this website? This soft-deletes it and disables widget access.')">@csrf @method('DELETE')<button class="inline-flex h-9 items-center rounded-lg border border-red-200 px-3 text-sm font-semibold text-red-600">Delete</button></form></div></td></tr>@empty<tr><td colspan="6" class="px-6 py-16 text-center text-sm text-slate-500">No websites found.</td></tr>@endforelse</tbody></table></div></section><div class="mt-6">{{ $websites->links() }}</div>
    </div></div>
</x-app-layout>
